{
  "name": "sydney-cartogram",
  "private": true,
  "scripts": {
    "build:data": "python3 build_commute_site_data.py",
    "dev": "python3 -m http.server 8000",
    "build": "mkdir -p dist && cp -R site/* dist/"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
